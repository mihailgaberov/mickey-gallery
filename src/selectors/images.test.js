import { getPageNum, getPhotos, saveImages, savePageNum } from './images'


describe('Images selectors', () => {
  describe('saveImages', () => {
    it('should record the images data into the state', () => {

    })
  })

  describe('getPhotos', () => {
    it('should return the images data from the state', () => {

    })
  })

  describe('getPageNum', () => {
    it('should return the number of the page to be used in the query to flickr api', () => {

    })
  })

  describe('savePageNum', () => {
    it('should save the number of the page to be used in the query to flickr api', () => {

    })
  })
})